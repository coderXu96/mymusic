export class MUSICLIST {
  constructor(image, name, playCount, linkurl, playhover) {
    this.image = image
    this.name = name
    // 播放量
    this.playCount = playCount
    // 跳转链接
    this.linkurl = linkurl
  }
}